# ============================================
# SERVER CONFIGURATION
# ============================================
server.port=8080
server.servlet.context-path=/api

# ============================================
# DATABASE CONFIGURATION (MySQL)
# ============================================
spring.datasource.url=jdbc:mysql://localhost:3306/ubakapp_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Q1w2e3r4t5@SQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ============================================
# JPA / HIBERNATE CONFIGURATION
# ============================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# CORRECT DIALECT FOR HIBERNATE 7.x
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect


# Fix for Hibernate 7.x
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# ============================================
# LOGGING
# ============================================
# Enable Spring logging
logging.level.org.springframework.web=DEBUG
logging.level.com.ubakapp=DEBUG
logging.level.org.springframework.boot.autoconfigure=DEBUG

# Disable static resource mapping for API paths
spring.mvc.static-path-pattern=/static/**

# Application properties
#server.servlet.context-path=/api

